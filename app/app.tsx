import { Card, Drawer, Button, Empty, Col } from 'antd';
import { hot, cold } from 'react-hot-loader'; // needs to be before react!
import * as React from 'react';
import { Game } from './components/game';


export default hot(module)(Game);
